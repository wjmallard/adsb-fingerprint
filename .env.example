# No credentials are needed yet: Postgres uses local peer auth (dbname only),
# and the RTL-SDR is local. This file is reserved for later secrets
# (e.g. an external ADS-B feed API key). Copy to .env when that day comes.
